import React from "react";
import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    oneInputStyles: {
        flexDirection: "row",
        marginHorizontal: 20
    },
})
